version: '3.8'

services:
  # =============================================
  # 1. BASE DE DATOS POSTGRESQL
  # =============================================
  postgres:
    image: postgres:16-alpine
    container_name: millete-db
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C"
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      # Escapamos las variables con doble $$ para que se resuelvan dentro del contenedor Linux
      test: ["CMD-SHELL", "pg_isready -U $${DATABASE_USER} -d $${DATABASE_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - millete-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================
  # 2. BACKEND (SPRING BOOT + JAVA 25)
  # =============================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: millete-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      # Conexión JDBC apuntando al servicio 'postgres'
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${DATABASE_NAME}
      SPRING_DATASOURCE_USERNAME: ${DATABASE_USER}
      SPRING_DATASOURCE_PASSWORD: ${DATABASE_PASSWORD}
      # Pasamos las claves de cifrado de Jasypt y JWT si las usas en tus yml
      JASYPT_ENCRYPTOR_PASSWORD: ${JASYPT_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      # Configuración CORS adaptada al puerto del frontend expuesto en el host
      # CORS_ALLOWED_ORIGINS: http://localhost:${FRONTEND_PORT:-3000}
      # Parámetros JVM óptimos para Java 25
      JAVA_OPTS: "-Xmx512m -Xms256m"
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-docker}
      LOGGING_LEVEL_ROOT: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      # Como no usas Actuator en el pom, comprobamos si el puerto 8080 está abierto y escuchando
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.01/8080 && exec 3<&-"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - millete-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================
  # 3. FRONTEND (REACT + VITE + PNPM + NGINX)
  # =============================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/api/v1}
        VITE_APP_VERSION: ${VITE_APP_VERSION:-0.0.1}
    container_name: millete-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    depends_on:
      backend:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - millete-network

# =============================================
# VOLÚMENES PERSISTENTES UNIFICADOS
# =============================================
volumes:
  postgres_data:
    name: millete_postgres_data
    labels:
      com.millete.description: "Datos persistentes de PostgreSQL"
      com.millete.backup: "required"

# =============================================
# RED INTERNA
# =============================================
networks:
  millete-network:
    name: millete_network
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
